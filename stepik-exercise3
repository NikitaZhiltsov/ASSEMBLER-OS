// Put your code here

subq $32, %RSI    // x - 32 result in RSI. Отнимаем 32 от того что пришло на вход в регистр RSI.
movq $5, %RAX    // 5 помещаем в RAX (там всегда хранится множитель. Он будет домнажать число которое хранится в регистре строчкой ниже)
mulq %RSI        // 5*(x - 32) Умножаем то, что было в RSI. Автоматически будет домножен на RAX. И сохранится в RAX. Старшие 64 бита.
                 // Буквально RSI*RAX, где в RSI результат предыдущего вычисление автоматически домножен RAXом.
movq $9, %R15    // 9 in R15 (там теперь записан делитель. Им мы будем делить то что теперь в RAXе).
divq %R15       // 5*(x-32)/9 result in RAX (то, что хранится в RAX делится на записанное в R15 и записывется в RAX - целое частное. )
                 // Буквально RAX/R15.
movq %RAX, %RSI   // освобождаем регистр RAX. По условию перемещаем в RSI
